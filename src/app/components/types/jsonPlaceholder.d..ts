namespace JsonPlaceholder {
    type Post = {
        userId: number,
        id: number,
        title: string,
        body: string
    }

    type Photo = {
        albumId: number,
        id: number,
        title: string,
        url: string,
        thumbnailUrl: string
    }
}
